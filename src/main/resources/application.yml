spring:
  main:
    banner-mode: off
  application:
    name: "kalaha"
  data:
    mongodb:
      database: ${spring.application.name}
      host: mongodb
      port: 27017
      auto-index-creation: true
server:
  servlet:
    contextPath: /api/kalaha